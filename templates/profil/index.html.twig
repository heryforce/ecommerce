{% extends 'base.html.twig' %}

{% block title %}
	{% trans %}Votre profil{% endtrans %}
{% endblock %}

{% block body %}

	<h1 class="bg-light text-center my-3">{% trans %}Votre profil{% endtrans %}</h1>

	{# AJOUTER LA CONDITION : SI L'UTILISATEUR N'A JAMAIS PASSE DE COMMANDE #}

	{# {% if produits | length == 0 %} #}

	<h2 class="text-center">{% trans %}Votre historique de commande est vide !{% endtrans %}
	</h2>

	{# {% else %} #}

	<table
		class="table">{# AFFICHER L'HISTORIQUE DES COMMANDES DE L'UTILISATEUR #}
	</table>

	{# {% endif %} #}

	<a onclick="return confirm('Voulez-vous supprimer votre profil ? Cette action est irréversible.')" href="{{ path('profil_delete', {'id': app.user.id}) }}" class="btn btn-danger btn-lg w-100">
		{% trans %}Supprimer mon profil{% endtrans %}</a>

{% endblock %}
